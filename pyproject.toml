[project]
name = "video2txt"
version = "0.1.0"
description = "把视频提交给AI分析输出文本结果的脚本"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-google-genai>=2.0.10",
    "moviepy>=2.1.2",
    "python-dotenv>=1.0.1",
]
